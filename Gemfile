# frozen_string_literal: true
source "https://rubygems.org"

gem "sinatra-activerecord"
# gem "sqlite3", '~> 1.3.6' # commented this out - is the original
gem "sqlite3" # added this based on the error message: Gem::LoadError: can't activate sqlite3 (~> 1.4), already activated sqlite3-1.3.13. Make sure all dependencies are added to Gemfile.
gem "pry"
gem "require_all"
gem 'rest-client'

